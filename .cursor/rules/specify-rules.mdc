# nexus Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-02-09

## Active Technologies
- TypeScript (strict), Bun + React, TanStack Router, React Query, Zustand, React Flow, tRPC, Fastify, Prisma (001-node-system)
- PostgreSQL via Prisma (packages/db); Node model added to schema (001-node-system)
- TypeScript 5.x (strict), React 19 + TanStack Router, TanStack Query, tRPC, shadcn/ui (Tailwind), Vercel AI SDK (@ai-sdk/react), existing ai-elements (Conversation, PromptInput, Message), react-resizable-panels, Zustand (002-workspace-split-view)
- PostgreSQL via Prisma (existing); client-side layout/selection state in localStorage only (002-workspace-split-view)
- TypeScript 5.x (strict mode, per CLAUDE.md) + React 19, Lexical + @lexical/react, tRPC, Prisma, TanStack Query, TailwindCSS, shadcn/ui (001-notes-markdown-editor)
- PostgreSQL via Prisma; Note content stored as JSON (Lexical EditorState serialization) (001-notes-markdown-editor)
- TypeScript 5.x, strict mode enabled across all packages + Backend: Fastify 5, tRPC 11, Prisma 7, `ai` v6, `@ai-sdk/google`. Frontend: React 19, TanStack Query 5, TailwindCSS, shadcn/ui. Shared: Zod, `packages/config` for SUMMARY_THRESHOLD. (003-context-engine)
- PostgreSQL via Prisma (`packages/db`); new Node/Message fields and optional app config env. (003-context-engine)

- TypeScript (strict) on Bun runtime + Fastify (backend HTTP server), tRPC (type-safe API layer), Prisma (ORM), (001-workspace-management)

## Project Structure

```text
backend/
frontend/
tests/
```

## Commands

npm test && npm run lint

## Code Style

TypeScript (strict) on Bun runtime: Follow standard conventions

## Recent Changes
- 003-context-engine: Added TypeScript 5.x, strict mode enabled across all packages + Backend: Fastify 5, tRPC 11, Prisma 7, `ai` v6, `@ai-sdk/google`. Frontend: React 19, TanStack Query 5, TailwindCSS, shadcn/ui. Shared: Zod, `packages/config` for SUMMARY_THRESHOLD.
- 001-notes-markdown-editor: Added TypeScript 5.x (strict mode, per CLAUDE.md) + React 19, Lexical + @lexical/react, tRPC, Prisma, TanStack Query, TailwindCSS, shadcn/ui
- 002-workspace-split-view: Added TypeScript 5.x (strict), React 19 + TanStack Router, TanStack Query, tRPC, shadcn/ui (Tailwind), Vercel AI SDK (@ai-sdk/react), existing ai-elements (Conversation, PromptInput, Message), react-resizable-panels, Zustand


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
